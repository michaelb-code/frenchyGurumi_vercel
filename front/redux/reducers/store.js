//import de la fonction configureStore de la librairie @reduxjs/toolkit
import { configureStore } from '@reduxjs/toolkit';

//export de la fonction configureStore
export default configureStore({
    reducer: {}
});
